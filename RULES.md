# LLM Wiki Agent 지침서 (AGENTS.md)

이 문서는 LLM 에이전트가 Markdown-only LLM Wiki를 유지보수(Maintenance)하고, 사용자 질의(User Query)에 응답하며, 지식 체계(Scheme)를 운영하기 위해 반드시 준수해야 하는 **핵심 룰북(Rulebook)** 입니다.
모든 LLM 에이전트는 이 파일의 지침을 읽고 엄격하게 준수해야 합니다.

---

## 1. Scheme 운영 방안 (Architecture & Roles)

LLM Wiki는 3개의 핵심 레이어로 운영됩니다. 에이전트는 이 레이어들의 역할을 침범하지 않고 유지해야 합니다.

*   `raw/` 폴더: **(불변 자료실)** 사용자가 주입하는 논문, 웹 스크랩, 회의록 등의 원본 파일이 들어옵니다. 에이전트는 이 폴더의 내용을 **절대 수정하거나 삭제할 수 없으며 오직 읽기(Read-only)**만 가능합니다.
*   `wiki/` 폴더: **(컴파일된 지식망)** 에이전트가 `raw/`에서 획득한 지식을 정리하여 보관하는 곳입니다. 에이전트가 읽고 쓰며(Read/Write) 자율적으로 마크다운 파일을 생성, 병합, 링크합니다.
*   `AGENTS.md` 파일: 현재 문서입니다. 에이전트의 작동 원칙입니다.

## 2. LLM Maintenance: 위키 생성 및 유지보수 규칙

에이전트가 `raw/` 폴더에 새로운 파일이 주입되었음을 감지했을 때, 다음의 **컴파일 및 유지보수 파이프라인 규칙**을 따릅니다.

### 2.1 문서 작성 (Ingestion & Compilation)
1.  **엔티티 분리:** 원본 문서가 다루는 핵심 개념, 이벤트, 인물 등을 파악하여 별도의 Markdown 엔티티 파일(예: `wiki/Attention_Mechanism.md`)로 분리 생성합니다.
2.  **파일 헤더 (Frontmatter):** 모든 `.md` 파일은 최상단에 다음 메타데이터를 포함해야 합니다.
    ```yaml
    ---
    title: [문서의 공식 제목]
    tags: [관련 태그1, 관련 태그2]
    created: [YYYY-MM-DD]
    updated: [YYYY-MM-DD]
    ---
    ```
3.  **한 줄 요약 (Summary):** 문서 본문 시작 시 반드시 `> [!INFO]` 또는 `> ` 블록을 사용하여 전체 내용을 1~2문장으로 요약합니다.
4.  **출처 표기 (Provenance):** 문서 최하단에 항상 `## Sources` 섹션을 만들고 `raw/` 내의 어떤 파일에서 추출한 지식인지 정확한 파일명과 경로를 기록합니다.

### 2.2 상호 연결 (Bookkeeping & Cross-referencing)
*   **강제 링크 생성:** 에이전트는 본문을 작성할 때, `wiki/` 폴더 내에 이미 존재하는 다른 마크다운 파일의 제목(개념)이 언급되면 반드시 대괄호 링크 구조인 `[[파일명]]` (예: `[[Attention_Mechanism]]`)으로 감싸야 합니다.
*   **새로운 개념 링크:** 현재 존재하지 않는 개념이라도 미래에 작성될 가치가 있는 핵심 명사라면 `[[새로운_개념]]` 형태로 껍데기 링크(Orphan link)를 남겨둡니다.

### 2.3 모순 해결 및 업데이트 (Conflict Resolution & Linting)
*   **기존 지식과 병합 (Merge):** 새 문서의 내용이 기존 `wiki/` 파일의 내용과 연관된다면, 기존 파일을 열고 적절한 헤딩(`## 추가 연구`, `## 2026년 업데이트` 등) 아래에 내용을 추가합니다. 기존 내용을 덮어씌워 훼손하지 않습니다.
*   **모순 제어:** 새 정보가 기존 정보와 명백히 충돌할 경우, 기존 정보를 삭제하지 말고 "대립되는 견해" 또는 "모순된 정보"라는 명시적인 알림과 함께 두 가지 정보를 병기하고 출처를 명확히 비교합니다.

## 3. User Query: 사용자 질의 응답 가이드

사용자가 지식과 관련된 질문을 할 때, 에이전트는 일반적인 방대한 사전 학습 지식(Pre-trained knowledge)에 의존하기 전에, **반드시 `wiki/` 내의 컴파일된 마크다운을 최우선 컨텍스트로 삼아야 합니다.**

1.  **Index First 탐색:** 질문이 들어오면 에이전트는 무작정 전체 파일을 읽기 전에, 항상 `wiki/index.md` (마스터 맵)를 열어 관련 엔티티가 어느 파일에 위치하는지 먼저 파악합니다.
2.  **Selective Context Loading:** 파악된 관련 `wiki/` 마크다운 파일만 컨텍스트로 불러와 읽습니다.
3.  **근거 제시:** 답변을 작성할 때, `wiki/` 내의 어떤 파일(`[[파일명]]`)을 참고하여 도출한 결과인지 사용자에게 반드시 출처(Citation)를 밝힙니다.
4.  **미컴파일 경고:** 만약 사용자가 물어본 내용이 `wiki/` 폴더 내에 없지만 일반 지식으로 답변할 경우, "해당 내용은 현재 개인 위키에 컴파일되어 있지 않으며, 에이전트의 기본 지식을 바탕으로 작성되었습니다"라고 경고합니다.
